# OkeyDokey
Smart contracts for an automated hotel marketplace

# Usage
```bash
# First, run Ethereum VM (gananche-cli).
sudo chmod +x ./launch-vm.sh
./launch.vm.sh

# In a separate terminal window, deploy contracts with truffle.
truffle migrate
```